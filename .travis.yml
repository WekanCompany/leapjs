language: node_js

node_js:
  - 13
  - 12
  - 10

jobs:
  include:
    - stage: release
      node_js: lts/*
      deploy:
        provider: script
        skip_cleanup: true
        script:
          - npm run publish
        on:
          branch: master
          tags: false
